name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "33 4 * * 1"

permissions:
  contents: read

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      # Free for personal repos. If this repo ever moves under an organization,
      # gitleaks-action requires a GITLEAKS_LICENSE — or rely on GitHub's native
      # secret scanning (see the hardening checklist in SECURITY.md).
      - uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dependency-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install shipped runtime dependencies
        run: pip install -c requirements/runtime-constraints.txt -e ".[postgres]"
      - name: Audit dependencies for known vulnerabilities
        run: |
          pip install pip-audit
          pip-audit
      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci
      # The REQUIRED gate audits only what actually ships (production deps): a new
      # advisory in a build-only tool (vite/eslint/etc.) must not block every merge.
      - name: Audit shipped frontend dependencies
        working-directory: frontend
        run: npm audit --omit=dev --audit-level=high
      # The full dev tree is still audited, but informationally: advisories surface
      # in the log without blocking, and are triaged rather than merge-gating.
      - name: Audit frontend dev tooling (informational)
        working-directory: frontend
        run: npm audit --audit-level=high
        continue-on-error: true
